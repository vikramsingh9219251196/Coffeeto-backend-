{
    "version": 2,
    "name": "coffeeto",
    "functions": {
      "api/*": {
        "handler": "server.js",
        "runtime": "@vercel/node12"
      }
    },
    "routes": [
      { "src": "/(.*)", "dest": "/server.js" }
    ]
  }